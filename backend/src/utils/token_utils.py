"""
Token utility functions have been removed as JWT logic has been removed from the system.
This file is kept for compatibility but contains no functional token utilities.
"""
import logging

logger = logging.getLogger('auth')

# All JWT-related functions have been removed as part of removing JWT logic from the system